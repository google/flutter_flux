name: flutter_flux_example
description: A new Flutter project.

dependencies:
  flutter:
    sdk: flutter
  flutter_flux:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

environment:
  sdk: '>=2.12.0 <3.0.0'
